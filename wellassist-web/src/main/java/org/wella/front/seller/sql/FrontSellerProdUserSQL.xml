<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="org.wella.front.seller.mapper.FrontSellerProdUserMapper">
	<!-- select -->
	<!-- 特定买家商品目录获得函数 -->
	<select id = "getUserProdList" parameterType = "java.util.Map" resultType = "java.util.Map">
		SELECT a.user_id, b.* , a.log_id
		FROM wa_prod_user a 
		INNER JOIN wa_prod b ON a.prod_id = b.prod_id 
		WHERE a.user_id IN (${userInStr})
		ORDER BY a.user_id,b.prod_id
	</select>	
	<!-- select -->
</mapper>