<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.wella.front.mapper.UserMapper">

	<select id="getUserInfo" parameterType="java.util.Map"
		resultType="java.util.Map">
		SELECT *
		FROM wa_user
		WHERE
			(emailAddr='${userid}' OR mobileAddr='${userid}')
			 AND regPass=md5('${userpass}')
			 AND userType=${useractor}
	</select>

	<!-- 订单审核列表个数获得SQL -->
	<select id="getDdShListCount" parameterType="java.util.Map"
		resultType="int">
		SELECT COUNT(*)
		FROM zh_order
		<where>
			<if test="orderNo!=null">
				AND order_no LIKE CONCAT('%', '${orderNo}', '%')
			</if>
			<if test="startOrderDate!=null">
				AND DATE(order_date) &gt;= #{startOrderDate}
			</if>
			<if test="endOrderDate!=null">
				AND DATE(order_date) &lt;= #{endOrderDate}
			</if>
			<if test="cgrNo!=null">
				AND cgr_no LIKE CONCAT('%', '${cgrNo}', '%')
			</if>
			<if test="cgrMc!=null">
				AND cgr_mc LIKE CONCAT('%', '${cgrMc}', '%')
			</if>
		</where>
	</select>

	<!-- select -->
</mapper>