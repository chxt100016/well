<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="org.wella.houtai.notify.mapper.HoutaiNotifyMapper">
	<!-- 获取订单列表 -->
	<select id="getNotifyList" parameterType="java.util.Map" resultType="java.util.Map">
		SELECT a.* FROM wa_info as a
			<where>
				<if test="notifyName!=null">
					AND a.tx_name LIKE CONCAT('%', '${notifyName}', '%') 
				</if>
				<if test="notifyType!=null">
					AND a.tx_type = ${notifyType} 
				</if>
			</where>
			<if test = "pageNum!=null">${pageNum} </if>
	</select>
	
	<select id="getNotifyListCount" parameterType="java.util.Map" resultType="int">
		SELECT count(*) FROM wa_info as a
			<where>
				<if test="notifyName!=null">
					AND a.tx_name LIKE CONCAT('%', '${notifyName}', '%') 
				</if>
				<if test="notifyType!=null">
					AND a.tx_type = ${notifyType} 
				</if>
			</where>	
	</select>
</mapper>