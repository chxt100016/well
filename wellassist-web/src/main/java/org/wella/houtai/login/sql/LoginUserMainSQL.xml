<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.wella.houtai.login.mapper.LoginUserMainMapper">
	<!-- zh_order -->
	<!-- get user main info by user name -->
	<select id="getUserMainInfo" parameterType="java.util.Map"
		resultType="java.util.Map">
		SELECT
		*
		FROM
		wa_admin
		WHERE
		adminName = '${userName}'
	</select>
	
	<select id="getAdminTypeInfo" parameterType="java.util.Map"
		resultType="java.util.Map">
		SELECT * FROM wa_user_role WHERE 1
	</select>	
	

	<!-- select -->
</mapper>