<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd"> 

<mapper namespace="org.wella.houtai.sxgl.mapper.HoutaiSxglUserMapper">
  	<!-- select -->
  	<!-- 放款放目录获得函数 -->
	<select id = "getFkfList" parameterType="java.util.Map" resultType = "java.util.Map">
	    SELECT * 
		FROM wa_user a 
		INNER JOIN wa_userinfo b ON a.user_id = b.user_id 
		WHERE user_type = 2 
			<if test = "companyName != null">
				AND b.company_name LIKE '%${companyName}%'  
			</if>
	   ORDER BY a.user_id DESC  
	   LIMIT ${start}, ${size}
	</select>
	<!-- 放款放目录个数获得函数  -->
	<select id = "getFkfListCount" parameterType="java.util.Map" resultType = "int">
	   SELECT COUNT(*) 
	   FROM wa_user a 
		INNER JOIN wa_userinfo b ON a.user_id = b.user_id 
		WHERE user_type = 2 
			<if test = "companyName != null">
				AND b.company_name LIKE '%${companyName}%'  
			</if>
	</select>
	<!-- select -->  
	
	
	
	
	 
</mapper>