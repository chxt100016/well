<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.wella.houtai.notice.mapper.HoutaiNoticeMapper">
	<!-- 获取订单列表 -->
	<select id="getNoticeList" parameterType="java.util.Map" resultType="java.util.Map">
		SELECT a.* FROM wa_notice as a  where (1=1)
			<where>
				<if test="noticeName!=null">
					AND a.notice_title LIKE CONCAT('%', '${noticeName}', '%') 
				</if>
				<if test="noticeType!=null">
					AND a.notice_type = ${noticeType} 
				</if>
			</where>
			<if test = "pageNum!=null">${pageNum} </if>
	</select>
	
	<select id="getNoticeListCount" parameterType="java.util.Map" resultType="int">
		SELECT count(*) FROM wa_notice as a  where (1=1)
			<where>
				<if test="noticeName!=null">
					AND a.notice_title LIKE CONCAT('%', '${noticeName}', '%') 
				</if>
				<if test="noticeType!=null">
					AND a.notice_type = ${noticeType} 
				</if>
			</where>	
	</select>
</mapper>