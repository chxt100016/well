<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.wella.front.mapper.FrontUserVehicleMapper">
	<!-- select -->
	<!-- 获得抢单RecentList的函数 -->
	<select id = "getRecentQdList"  resultType = "java.util.Map">
		SELECT a.*, b.* 
		FROM wa_user_vehicle a 
		INNER JOIN wa_order b ON a.order_id = b.order_id 
		WHERE vehicle_state = 1 
		ORDER BY a.create_date DESC
	</select>
	
	<!-- 物流订单详细信息获得的 -->
	<select id = "getVehicleInfo" parameterType = "java.util.Map" resultType = "java.util.Map">
		SELECT * 
		FROM wa_user_vehicle a 
		INNER JOIN wa_order b ON a.order_id = b.order_id
		INNER JOIN wa_prod c ON b.prod_id = c.prod_id 
		INNER JOIN wa_user d ON a.user_id = d.user_id 
		WHERE a.vehicle_trans = #{vehicleTrans}
	
	</select>
</mapper>