<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.wella.front.mapper.NewsMapper">

	<!-- 获取订单列表 -->
	<select id="getNewsList" parameterType="java.util.Map" resultType="java.util.Map">
		SELECT a.tx_id, a.tx_name, a.tx_content, a.tx_date, a.is_read FROM wa_info as a where user_id=${userId} order by fs_date desc
			LIMIT ${start} , ${size}
	</select>
	
	<select id="getNewsListCount" parameterType="java.util.Map" resultType="int">
		SELECT count(*) FROM wa_info as a  where user_id=${userId}	
	</select>
</mapper>