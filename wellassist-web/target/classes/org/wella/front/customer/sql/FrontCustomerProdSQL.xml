<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="org.wella.front.customer.mapper.FrontCustomerProdMapper">
	<!-- 获得商品信息的函数 -->				
	<select id="getProdInfo" parameterType="java.util.Map"  resultType = "java.util.Map"  >
		SELECT * 
		FROM wa_prod 
		<where>
			<if test = "prodId != null">
				AND prod_id = #{prodId}
			</if>
		</where>
	</select>
</mapper>
